# Der Zugang des Postboten: das Erneuerungs-Token ist neunzig Tage lang der
# Schlüssel zu allem, was die App weiß. Es gehört auf diesen Rechner und
# nirgendwo sonst.
zugang.json

# Welche Blätter schon einen Lauf hatten. Kein Geheimnis, aber auch nichts,
# was jemand anderes brauchen könnte — es gilt nur für dieses Gerät.
gesehen.json

# Die Sperre, die zwei gleichzeitige Postboten verhindert.
lauf.lock
